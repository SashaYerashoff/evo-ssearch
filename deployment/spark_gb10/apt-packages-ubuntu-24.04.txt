# EVA AI application and offline installation (Ubuntu 24.04 ARM64)
ca-certificates
curl
ffmpeg
jq
nginx
openssl
postgresql
postgresql-contrib
python3
python3-dev
python3-pip
python3-venv
rsync
tzdata

# CPU llama.cpp build
build-essential
cmake
ninja-build
pkg-config

# Spark ships its kernel, NVIDIA driver and CUDA/PyTorch integration as one
# vendor-tested stack. The EVA bundle must validate and preserve that stack;
# it must not install an Ubuntu desktop NVIDIA driver or an HWE kernel over it.
pciutils
v4l-utils

# Field diagnostics and host maintenance
htop
nvme-cli
smartmontools
ufw
usbutils
