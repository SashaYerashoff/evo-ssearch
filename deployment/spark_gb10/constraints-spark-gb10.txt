# Python dependency contract for EVA on Ubuntu 24.04 ARM64.
#
# torch, torchvision and NumPy are supplied by the pinned NVIDIA Spark runtime.
# The installer validates their exact versions before mutation and creates
# EVA's venv with access to the vendor system packages.  Keep this NumPy pin
# aligned with /etc/pip/constraint.txt inside that image.
setuptools==80.10.2
transformers==5.13.1
numpy==2.1.0
opencv-python-headless==5.0.0.93
pillow==12.3.0
